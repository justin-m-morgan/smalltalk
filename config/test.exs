import Config

config :smalltalk, token_signing_secret: "qi5KgbihV2wVx543goMlva/KrShLv9Xx"

# Always prefer the `apps/<app_name>.exs` configuration over the environment file
# Generators may require keys to exist here but should be promptly moved to specific file
